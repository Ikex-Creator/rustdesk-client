[package]
name = "sit-managed-cli-tests"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
zeroize = "=1.8.1"

[target.'cfg(windows)'.dependencies]
windows = { version = "=0.61.1", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_Storage_FileSystem",
    "Win32_System_IO",
    "Win32_System_Pipes",
] }
